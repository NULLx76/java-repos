<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.ezrebclan</groupId>
  <artifactId> ezrebutils</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>EzrebUtils</name>
  <description>Its a pretty basic and simple bit of string/array utilities, which, while being really simple, since I use them a lot, I decided to make them into their own project :P</description>
  <build>
  	<plugins>
  		<plugin>
  			<artifactId>maven-assembly-plugin</artifactId>
  			<executions>
  				<execution>
  					<id>release-full</id>
  					<goals>
  						<goal>assembly</goal>
  					</goals>
  					<phase>package</phase>
  					<configuration>
  						<descriptor>src/assembly/release-full.xml</descriptor>
  						<appendAssemblyId>false</appendAssemblyId>
    			  		<finalName>${project.artifactId}-${project.version}-release</finalName>
  					</configuration>
  				</execution>
  				<execution>
  					<id>release-dev</id>
  					<goals>
  						<goal>attached</goal>
  					</goals>
  					<phase>package</phase>
  					<configuration>
  						<descriptor>src/assembly/release-dev.xml</descriptor>
  						<appendAssemblyId>false</appendAssemblyId>
    			  		<finalName>${project.artifactId}-${project.version}-development</finalName>
  					</configuration>
  				</execution>
  			</executions>
  		</plugin>
  		<plugin>
  			<groupId>org.apache.maven.plugins</groupId>
  			<artifactId>maven-compiler-plugin</artifactId>
  			<version>3.3</version>
  			<configuration>
  				<source>1.7</source>
  				<target>1.7</target>
  			</configuration>
  		</plugin>
  		<plugin>
  			<artifactId>maven-clean-plugin</artifactId>
  			<configuration>
  				<filesets>
  					<fileset>
		  				<directory>${project.basedir}/target</directory>
  						<includes>
  							<include>${project.artifactId}-${project.version}.jar</include>
  						</includes>
  						<excludes>
  							<exclude>archive-tmp</exclude>
  							<exclude>generated-sources</exclude>
  							<exclude>maven*</exclude>
  							<exclude>*classes</exclude>
  							<exclude>${project.artifactId}-${project.version}-release.zip</exclude>
  							<exclude>${project.artifactId}-${project.version}-development.zip</exclude>
  							<exclude>/</exclude>
  							<exclude>site*</exclude>
  						</excludes>
  					</fileset>
  				</filesets>
  				<excludeDefaultDirectories>true</excludeDefaultDirectories>
  			</configuration>
  		</plugin>
  		<plugin>
  			<artifactId>maven-jar-plugin</artifactId>
  			<executions>
  				<execution>
  					<id>development</id>
  					<phase>jar:jar</phase>
  					<configuration>
  						<finalName>${project.artifactId}-${project.version}-DEVELOPMENT</finalName>
  					</configuration>
  				</execution>
  				<execution>
  					<id>main</id>
  					<phase>package</phase>
  					<configuration>
  						<finalName>${project.artifactId}-${project.version}</finalName>
  					</configuration>
  				</execution>
  			</executions>
  		</plugin>
  	</plugins>
  </build>
</project>